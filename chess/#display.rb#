require 'colorize'
require_relative 'cursor.rb'

class Display:Q:Qlpwemkfg;aowigjn;aeorign;ewkFL;L:P:Q;qef[l;,aœôø±uuu c:qL!:::
  def initialize(board)
    @board = board
    @cursor = Cursor.new([0,0], board)
  end

  def render

    # @board.grid.each_with_index do |row, indx|
    #   puts "0 1 2 3 4 5 6 7 " if indx==0
    #   row.each do |tile|
    #
    #   end
    # end

    while true
      @cursor.get_input
    end
  end



end
